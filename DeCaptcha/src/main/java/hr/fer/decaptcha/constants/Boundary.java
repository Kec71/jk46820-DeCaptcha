package hr.fer.decaptcha.constants;

/**
 * Enum which represents boundry.
 * This enum is used while determining upper or lower boundry detection from histogram.
 * @author Janko
 *
 */
public enum Boundary {
	UPPER, BOTTOM;
}
